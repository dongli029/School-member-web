<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Delete faculty</title>
  </head>
  <body>
    <!-- action="/students代表要新增到method="post"的/students路徑 -->
    <form
      action="/faculties/<%= foundFaculty._id %>?_method=Delete"
      method="post"
    >
      <label for="name">Name</label>
      <input type="text" name="name" value="<%= foundFaculty.name %>" />
      <label for="age">Age</label>
      <input type="number" name="age" value="<%= foundFaculty.age %>" />
      <label for="suject">Suject </label>
      <input type="text" name="suject" value="<%= foundFaculty.suject %>" />
      <button>delete data</button>
      <a href="/faculties/<%= foundFaculty._id %>">Cancel</a>
    </form>
  </body>
</html>
